import React, { Component } from 'react';
import "./Style.css"

class Home extends Component {

    render() {
        return (
            <div className="content">
                <h2>As the next step in this Bootstrap tutorial, we want to include a full-screen background image for our landing page header. For that, we will have to use some jQuery to make the image stretch all the way across the screen</h2>
            </div>
        );
    }
}

export default Home;